---
- name: Get section from inventory
  ansible.builtin.set_fact:
    section: "{{ delete_sap_luns[sap_variant] }}"

- name: Schau me
  ansible.builtin.debug:
    msg: "{{ item.volume }}"
  loop: "{{ delete_sap_luns[sap_variant] | list }}"
